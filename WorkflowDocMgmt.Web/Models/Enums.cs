//Enums.cs
namespace WorkflowDocMgmt.Web.Models
{
    public enum AccessLevel { ReadOnly, ReadWrite }
    public enum WorkflowType { Order, Pool }
    public enum DocumentStatus { Pending, Approved, Rejected }
}
